<h1>Ordens de serviço</h1>
<ul>
    <% @ordems.each do |ordem| %>
        <li>
            <%= link_to Veiculo.find(ordem.veiculo_id).placa, ordem %>
            Valor:
            <%= ordem.valor %>
        <li>
    <% end %>
</ul>
<%= link_to "Nova Ordem de Serviço", new_ordem_path %>